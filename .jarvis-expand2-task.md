## Task: Expand Jarvis Metrics — Round 2 (Group Q)

All 3655 candidate experiments are exhausted. Add NEW indicator families to `buildings/jarvis/jarvis-metrics.js`, update `buildings/jarvis/rebuild-candidate-queue.js`, regenerate the queue, and commit.

### Context
- `buildings/jarvis/jarvis-metrics.js` — main metrics file (~136KB, ~2600 lines)
- `buildings/jarvis/rebuild-candidate-queue.js` — queue builder
- `buildings/jarvis/indicators.json` — 1420 existing atomics (DO NOT DUPLICATE)
- `buildings/jarvis/derived_experiments.json` — 12959 already-tried composites (queue builder excludes these automatically)
- `buildings/jarvis/candidate_queue.json` — currently 3655 entries, all exhausted

### Families ALREADY EXISTS (do not re-add):
zygarnik_buildup, zygarnik_score, zygarnik_tension, open_loop_*, closure_*, hook_*, proof_withheld, proof_density, setup_density, setup_payoff, payoff_density, payoff_position, micro_reward, micro_commitment, stakes_*, stake_*, story_stake, visual_proof, visual_stake, credential_signal, social_proof, delayed_gratification, gratification_delay, pre_closure, pre_proof, pre_payoff, consequence_*, loop_density, unresolved_loop, first_payoff, closing_hook

### STEP 1: Study the pattern in jarvis-metrics.js

Read jarvis-metrics.js lines 1-100 for the ZYGARNIK_PHRASE_SETS and helper functions.
Then read around lines 1600-2600 for the extractMetric if-else chain pattern.

Look at 2-3 existing metric blocks to understand the exact pattern. Each block looks like:

```javascript
if (key === 'metric_name') {
    const words = fullText.split(/\s+/);
    const count = countPhraseMatches(fullText, SOME_PHRASE_SET);
    return [count, null];
}
if (key === 'metric_density') {
    const [count, err] = extractMetric('metric_name', video);
    if (err) return [null, err];
    return [count / Math.max(words.length, 1), null];
}
```

### STEP 2: Add 7 NEW indicator families (Group Q) to extractMetric

Add these blocks to the extractMetric function body (inside the large if-else chain). Place them just before the final `return [null, 'unknown metric']` line.

**Family Q1: Anticipation language** (new phrase set needed: ANTICIPATION_PHRASES)
Phrases: "wait for it", "here's the thing", "you won't believe", "before i show you", "but first", "hold on", "here's what happened", "wait until you see", "you need to see this", "stay with me", "trust me on this", "this is where it gets", "it gets better", "it gets worse", "what happens next"

- `anticipation_phrase_count` — count in full transcript
- `anticipation_phrase_density` — count / word count  
- `anticipation_phrase_count_first10s` — count in first 10% of words
- `anticipation_front_load_ratio` — count in first 50% / (count in second 50% + 0.001)

**Family Q2: Counterintuitive/reveal signals** (new phrase set: COUNTERINTUITIVE_PHRASES)
Phrases: "actually", "surprisingly", "counterintuitively", "you'd think", "most people think", "the truth is", "turns out", "it turns out", "plot twist", "here's the twist", "not what you'd expect", "against all odds", "nobody expected", "the opposite", "contrary to", "what actually happened", "what really works", "what i found instead"

- `counterintuitive_count` — count in full transcript
- `counterintuitive_density` — count / word count
- `counterintuitive_count_first_half` — count in first 50% of words
- `counterintuitive_count_first10s` — count in first 10% of words

**Family Q3: Confession/vulnerability signals** (new phrase set: CONFESSION_PHRASES)
Phrases: "i was wrong", "i made a mistake", "i failed", "honestly", "truth is", "i struggled", "it wasn't easy", "i almost gave up", "i didn't know", "i had no idea", "embarrassing", "hard to admit", "i was scared", "i was nervous", "the real reason", "behind the scenes", "what nobody shows", "the ugly truth"

- `confession_signal_count` — count in full transcript
- `confession_signal_density` — count / word count
- `confession_first_half_count` — count in first 50% of words
- `confession_hook_count` — count in first 10% of words

**Family Q4: Escalation language** (new phrase set: ESCALATION_PHRASES)
Phrases: "and then", "but then", "what's worse", "it gets worse", "even more", "and on top of that", "to make matters worse", "but here's the thing", "that's when", "and that's when", "which means", "so naturally", "so i decided", "so what did i do", "the next step", "what happened next"

- `escalation_phrase_count` — count in full transcript
- `escalation_phrase_density` — count / word count
- `escalation_count_first_third` — count in first 33% of words
- `escalation_count_mid_third` — count in words from 33%-67%

**Family Q5: Specificity markers** (new phrase set: SPECIFICITY_PHRASES, plus number regex)
Number regex: /\b\d+(?:\.\d+)?(?:%|x|k|m|b|s|sec|min|hour|day|week|month|year)?\b/gi
Specificity phrases: "exactly", "specifically", "precisely", "in particular", "to be exact", "to be specific", "the specific", "the exact", "for instance", "for example", "such as", "like this", "here's an example", "case study", "data shows", "the numbers show", "the stat", "the statistic"

- `numeric_specificity_count` — count of number regex matches in full transcript
- `numeric_specificity_density` — count / word count
- `numeric_specificity_first_half` — count in first 50% of words
- `specificity_phrase_count` — count of SPECIFICITY_PHRASES in full transcript
- `specificity_phrase_density` — count / word count

**Family Q6: Narrative callback signals** (new phrase set: CALLBACK_PHRASES)
Phrases: "as i mentioned", "remember when", "earlier i said", "going back to", "like i said", "as we saw", "as you saw", "we talked about", "i talked about", "i mentioned earlier", "this connects to", "this ties back", "which brings us back", "full circle", "coming full circle"

- `callback_count` — count in full transcript
- `callback_density` — count / word count
- `callback_second_half_count` — count in second 50% of words (callbacks occur later)
- `callback_last_third_count` — count in last 33% of words

**Family Q7: Urgency/FOMO signals** (new phrase set: URGENCY_PHRASES)
Phrases: "don't miss", "watch till the end", "watch to the end", "stay till the end", "before it's too late", "limited time", "right now", "today only", "act now", "time is running out", "don't scroll past", "stop scrolling", "you need to watch", "most important", "the most important thing", "pay attention", "this matters", "this is crucial", "this changes everything"

- `urgency_signal_count` — count in full transcript
- `urgency_signal_density` — count / word count
- `urgency_count_first_quarter` — count in first 25% of words
- `urgency_count_last_quarter` — count in last 25% of words
- `urgency_front_load_ratio` — urgency in first 50% / (urgency in second 50% + 0.001)

### STEP 3: Add phrase sets near top of jarvis-metrics.js

Near the existing ZYGARNIK_PHRASE_SETS constant (around line 20-80), add the 7 new phrase set constants with the phrases from Step 2.

### STEP 4: Update rebuild-candidate-queue.js

In the `baseAtomics` array, add all new atomic keys (Family Q1-Q7):
'anticipation_phrase_count', 'anticipation_phrase_density', 'anticipation_phrase_count_first10s', 'anticipation_front_load_ratio',
'counterintuitive_count', 'counterintuitive_density', 'counterintuitive_count_first_half', 'counterintuitive_count_first10s',
'confession_signal_count', 'confession_signal_density', 'confession_first_half_count', 'confession_hook_count',
'escalation_phrase_count', 'escalation_phrase_density', 'escalation_count_first_third', 'escalation_count_mid_third',
'numeric_specificity_count', 'numeric_specificity_density', 'numeric_specificity_first_half', 'specificity_phrase_count', 'specificity_phrase_density',
'callback_count', 'callback_density', 'callback_second_half_count', 'callback_last_third_count',
'urgency_signal_count', 'urgency_signal_density', 'urgency_count_first_quarter', 'urgency_count_last_quarter', 'urgency_front_load_ratio'

### STEP 5: Run the queue builder

Run: node buildings/jarvis/rebuild-candidate-queue.js

Check the output. It should show the new candidate count (should be 1000+ new candidates).

### STEP 6: Commit and push

git add buildings/jarvis/jarvis-metrics.js buildings/jarvis/rebuild-candidate-queue.js buildings/jarvis/candidate_queue.json
git commit -m "Expand Jarvis metrics: anticipation/counterintuitive/confession/escalation/specificity/callback/urgency families (Group Q) + repopulate candidate queue"
git push origin master

### STEP 7: Notify when done

openclaw system event --text "Done: Expanded Jarvis metrics with 7 new families (Group Q), candidate pool ready for relaunch" --mode now

### CRITICAL RULES:
- DO NOT modify any other Jarvis JSON data files (indicators.json, graph.json, results.tsv, derived_experiments.json, autonomous_progress.json, autonomous_runs.json, resolutions.json)
- Use `countPhraseMatches(text, phraseArray)` helper — it already exists in the file
- Each metric MUST return [value, null] or [null, 'reason']
- Check that the rebuild script runs without errors before committing
- If rebuild-candidate-queue.js errors, fix it before committing
