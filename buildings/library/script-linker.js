/**
 * ScriptLinker — DEPRECATED stub.
 * Scripts are now embedded directly in ideas/videos.
 */
const ScriptLinker = (() => {
    return {
        renderLinker() { return ''; },
        renderPickerOverlay() { return ''; },
        bindEvents() {}
    };
})();
